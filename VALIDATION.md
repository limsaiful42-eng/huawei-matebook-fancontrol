# BIOS 1.22 逆向与实机验收记录

验收时间：2026-08-08（本机 Huawei MateBook 14 2024 / FlemingH）

## 固件结论

磁盘中没有找到可确认完整的 BIOS 1.22 更新胶囊，因此没有把旧版文件冒充 1.22。逆向依据是当前已安装 BIOS 1.22 导出的实时 ACPI 表；旧版 1.09 官方包只用于模块结构对照。固件和 ACPI 提取物不包含在公开仓库中。

实时表中的 `SFND` 方法确认了厂商风扇接口：

- `02 11 <fan> 01 <rpm-low> <rpm-high>`：进入风扇测试/手动模式并设置指定风扇转速。
- `02 11 00 00 00 00`：退出测试模式，恢复厂商自动控制。
- `02 02 05`：读取 CPU 温度。
- `02 08 00` / `02 08 01`：读取两把风扇的物理转速计。

标准 Intel DPTF `SET_FAN_LEVEL` 在这台机器上返回成功但没有改变控制值或物理 RPM，因此最终控制器没有采用该无效路径。

## 自动控制验收

原始验收日志包含本机路径，因此没有提交到公开仓库。以下是脱敏后的测试摘要：

- watchdog 故障注入：先观察到 5280/5160 RPM，再模拟主进程不存在；watchdog 第一次重试即成功退出手动模式，物理风扇降到 3840/3720 RPM，无后备恢复介入。
- 原始电源设置恢复并逐项核对：处理器最大状态 100/100，散热策略 1/0，Boost 2/2，EPP 33/40，AC/DC 实际值全部等于测试前快照。
- 正常功耗下自动控制：28 个样本，48–61 °C；曲线由 3200 RPM 自动升到 3800 RPM；结束时两把风扇均为 3900 RPM。
- 物理跟随性：两把风扇各有 26/28 个样本位于目标 ±300 RPM 内。
- 正常退出：主控制器成功恢复厂商自动模式，watchdog 收到正常停止信号；8 秒后的恢复样本为 3900/3840 RPM。

## 高转速请求校准

2026-08-23 使用每档 20 秒、最后五个样本平均值进行了三档对比：

| BIOS/EC 请求值 | Fan 0 稳态平均 | Fan 1 稳态平均 | 观测峰值（Fan 0 / Fan 1） |
|---:|---:|---:|---:|
| 7400 | 6984 RPM | 7032 RPM | 7020 / 7080 RPM |
| 9300 | 7080 RPM | 7092 RPM | 7080 / 7320 RPM |
| 12000 | 7140 RPM | 7044 RPM | 7140 / 7320 RPM |

测试最高温度 56 °C，退出手动模式和 watchdog 正常停止均成功。结论是 `_FPS` 中的高档 RPM 字段在此机型上不是物理转速保证值；约 7400 请求以后风扇已经接近物理/EC 饱和。历史 12000 请求的五分钟测试曾观察到约 7620/7440 RPM 的短时峰值，因此全速预设仍保留 12000 请求，但界面明确标为 `Request`。

## 单文件 EXE 验收

`HuaweiFanControl.exe` 1.1.0 分别通过了监测和实际控制两种一分钟端到端测试：

- 监测模式：内嵌资源成功释放，管理员 PowerShell 子进程正常读取 28 个样本，温度 56–58 °C，无写入、无错误并自然退出。
- 实际控制：28 个样本，温度 56–59 °C，自动曲线执行两次控制调整；两把风扇各有 25 个样本位于请求值 ±300 RPM，末端为 3840/3900 RPM。
- 恢复：实际控制测试结束时 BIOS 厂商自动模式恢复成功，独立 watchdog 收到正常停止信号。
- 打包：EXE 内嵌控制器、watchdog 和两条曲线，版本 1.1.0.0；构建过程生成独立 SHA-256 校验文件。

## 图形界面验收

`HuaweiFanControlUI.exe` 1.2.0 在本机高 DPI Windows 环境下通过了界面与实际控制验收：

- 界面：顶部状态、四个实时指标卡、四个模式按钮、全速时长、紧急温度和日志区在 1726×1248 捕获画面中均无裁切或重叠。
- 实际控制：由 UI 启动安静自动模式 30 秒，共记录 13 个样本，温度 62–64 °C，请求值由 3800 升到 5100 RPM。
- 双风扇跟随：转速范围分别为 3900–5340 RPM 和 3900–5460 RPM，结束时均为 5160 RPM。
- 安全退出：控制器确认进入手动模式并启动独立 watchdog；UI 定时发出停止信号后，总结中 `RestoreSucceeded=True`，watchdog 收到正常停止信号，UI 和所有子进程自行退出。
- 打包：GUI 内嵌控制器、watchdog、原厂恢复脚本和两条曲线，版本 1.2.0.0；运行时不需要外部 PowerShell 脚本。

项目没有写未知 EC RAM、没有修改 BIOS 镜像、没有刷写固件，也没有关闭 Secure Boot 或启用测试签名。
